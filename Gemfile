source "https://rubygems.org"

# Use github-pages for compatibility with GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Add webrick as it's no longer bundled with Ruby 3.0+
gem "webrick"

# Theme
gem "jekyll-theme-slate"

# Add these to address the warnings (uncomment if needed)
# gem "logger"
# gem "csv"
# gem "ostruct"
# gem "base64"

# Other commonly used plugins
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end